
services:
  backend:
    restart: always
    container_name: motor-control-hub-backend
    build:
      context: ./backend
    expose:
      - "8080"
    
  frontend:
    restart: always
    depends_on:
      - backend
    build:
      context: ./frontend
    ports:
      - "80:80"